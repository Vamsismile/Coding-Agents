# Dummy Project

This is a minimal Python dummy project created for testing.

Usage

- Run: `python app.py`

Files

- `app.py` — simple entrypoint that prints a message
- `requirements.txt` — dependencies (empty)
- `.gitignore` — ignores common Python artifacts
